import { Box, Card, CardMedia, Typography } from "@mui/material";
import React, { useState } from "react";

import "./Home.css";

const Home = () => {
  return (
    <Box className=" hero">
      <Box></Box>
    </Box>
  );
};

export default Home;
